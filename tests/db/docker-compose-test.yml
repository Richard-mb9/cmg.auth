version: "3.1"
services:
  cmg-auth-test:
    image: "postgres:alpine"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=cmg-auth-test
    expose:
      - "5432"
    ports:
      - "5432:5432"
  
