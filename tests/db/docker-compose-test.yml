version: "3.1"
services:
  cmg-auth-test:
    image: "postgres:latest"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=cmg-auth-test
    ports:
      - "5432:5432"
  
